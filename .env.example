# Copy to .env and fill in. NEVER commit .env.
# Claude Code sessions: never read .env contents — verify presence only, e.g.
#   awk -F= '/^ANTHROPIC_API_KEY=/ {print $1, "len:", length($2)}' .env
ANTHROPIC_API_KEY=

# Optional model override (default: claude-opus-4-8)
HALO_MODEL=
